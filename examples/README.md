# Examples

- `minimal_orchestrator.cpp` — registers one task type, runs a single-node blueprint, prints context output.

Configure with `-DTASKFLOW_BUILD_EXAMPLES=ON` (default) and build target `minimal_orchestrator`.
